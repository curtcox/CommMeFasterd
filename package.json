{
  "name": "comm-me-fasterd",
  "version": "0.1.0",
  "description": "Mac Electron shell for communication apps with automation hooks.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check renderer.js && node --check scripts/capture-automation-runner.js && node --check scripts/run-outlook-capture-automation.js && node --check scripts/run-teams-capture-automation.js && node --check scripts/run-teams-diagnose.js",
    "test": "node --test",
    "outlook:automation": "node scripts/run-outlook-capture-automation.js",
    "teams:automation": "node scripts/run-teams-capture-automation.js",
    "teams:diagnose": "node scripts/run-teams-diagnose.js"
  },
  "keywords": [
    "electron",
    "slack",
    "teams",
    "gmail"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "electron": "^40.6.1"
  }
}
